export const bookingConfirmed = (message) => {
  alert(message);
  alert("Booking confirmed! Sending confirmation...");

  // Simulate sending a confirmation message
  alert("Confirmation message sent to the user.");
};
